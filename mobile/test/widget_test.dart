// Pruebas de integración de la app — Sprint 1
// Las pruebas unitarias de cada feature viven en test/features/
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder: suite de pruebas base activa', () {
    expect(true, isTrue);
  });
}
